# ─────────────────────────────────────────────────────────────────────
# FORENSIC AI — Minimal Requirements (Colab / Quick Start)
# Agentic RAG architecture: LangChain + LlamaIndex + LangGraph
# Works with any LLM provider; DuckDuckGo search is free (no key).
#
# Install: pip install -r requirements-minimal.txt
# Then:    Set any one of: GROQ_API_KEY, GOOGLE_API_KEY, OPENAI_API_KEY, etc.
# ─────────────────────────────────────────────────────────────────────

# Agentic RAG core (required)
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0    # DuckDuckGo search (free, no key needed)
duckduckgo-search>=6.0.0

# LlamaIndex retrieval
llama-index>=0.11.0
llama-index-core>=0.11.0
llama-index-embeddings-huggingface>=0.3.0
llama-index-vector-stores-chroma>=0.2.0

# LLM providers (install ONE or more; all are fully supported — no free bias)
openai>=1.35.0              # OpenAI, Groq, LM Studio, OpenRouter (OpenAI-compatible)
google-generativeai>=0.7.0  # Google Gemini
anthropic>=0.31.0           # Anthropic Claude

# LangChain provider adapters matching the providers above
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langchain-anthropic>=0.2.0
langchain-groq>=0.2.0

# Core
python-dotenv>=1.0.0
loguru>=0.7.2
httpx>=0.27.0
requests>=2.31.0

# Data
yfinance>=0.2.40
pandas>=2.1.0
numpy>=1.26.0
beautifulsoup4>=4.12.3

# RAG
sentence-transformers>=3.0.0
chromadb>=0.5.0
rank-bm25>=0.2.2

# Document Processing
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23
reportlab>=4.2.0
Pillow>=10.3.0

# Database
duckdb>=1.0.0

# UI (optional — skip if CLI only)
streamlit>=1.36.0
plotly>=5.22.0

# Utilities
tqdm>=4.66.0
tenacity>=8.3.0
fuzzywuzzy>=0.18.0          # used by utils/helpers.py for company name matching
python-Levenshtein>=0.25.0  # accelerates fuzzywuzzy
